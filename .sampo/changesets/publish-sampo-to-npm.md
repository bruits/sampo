---
cargo/sampo: minor
npm/sampo: minor
---

Sampo is now published to npm. Install with `pnpm add -D sampo` (or `npm i -D sampo` / `yarn add -D sampo`) on Linux x64/arm64, macOS x64/arm64, or Windows x64, then run `pnpm sampo` to use it.
