[package]
name = "sampo"
version = "0.0.0"
edition = "2024"
readme = "README.md"
description = "Sampo CLI: automate changelogs, versioning, and publishing"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rand = "0.8"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
